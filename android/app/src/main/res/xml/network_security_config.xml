<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow plain HTTP to our VPS backend -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">187.77.14.62</domain>
    </domain-config>
    <!-- All other traffic follows system default (HTTPS only) -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
